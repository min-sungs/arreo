import React from 'react';
import styled from 'styled-components';

const Container = styled.div``;

const Call = () => {
  return <Container>소프트폰 - Call 페이지입니다.</Container>;
};

export default Call;
