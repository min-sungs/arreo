// import React from 'react';
// import BaseButton from '../../Atom/BaseButton';

// const LargeButtonGroup = ({ buttons }: any) => (
//   <div>
//     {buttons.map(({ label, onClick, ...buttonProps }) => (
//       <BaseButton key={label} onClick={onClick} {...buttonProps}>
//         {label}
//       </BaseButton>
//     ))}
//   </div>
// );

// export default LargeButtonGroup;
